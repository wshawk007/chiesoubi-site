name: Security contract

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify-site:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify static site and security headers
        run: python3 scripts/verify_site.py
