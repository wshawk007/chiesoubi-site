# CHIESOUBI site

Official static website source for `chiesoubi.jp`, deployed with Cloudflare Pages.

## Verification

```sh
python3 scripts/verify_site.py
```

Security headers are defined in `_headers` and checked by the `Security contract` GitHub Actions workflow.
